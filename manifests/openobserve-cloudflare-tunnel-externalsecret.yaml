apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-tunnel-openobserve-token
  namespace: openobserve
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: vault-store-openobserve
  target:
    name: cloudflare-tunnel-openobserve-token
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
  data:
    - secretKey: value
      remoteRef:
        # Vault KVv2 path: secret/data/cloudflare-tunnel-openobserve-token
        key: cloudflare-tunnel-openobserve-token
        property: value
